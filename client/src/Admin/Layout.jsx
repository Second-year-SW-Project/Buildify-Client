import { DashboardLayout } from '@toolpad/core';
import { Outlet } from 'react-router-dom';
import AccountDropdown from '../MoleculesComponents/Admin_components/AccountDropdown';

export default function Layout() {
    return (
        
            <Outlet />
    
    )
}

